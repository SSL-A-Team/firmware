[build]
target = "thumbv7em-none-eabihf"

[target.thumbv7em-none-eabihf]
runner = 'probe-rs run --chip STM32F407VETx --connect-under-reset'
# runner = 'probe-rs debug --chip STM32F407VETx --connect-under-reset --exe'

[env]
DEFMT_LOG = "trace"
